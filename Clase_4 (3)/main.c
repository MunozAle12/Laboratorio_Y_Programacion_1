#include <stdio.h>
#include <stdlib.h>

int main()
{
    int resultado;
    resultado = dividirEnteros(100,4);
    printf("%d", resultado);
    resultado = retornarValorEntre(-40, 40, &resultado);
    if(sePudo == 0)
    {
        printf("%d", resultado);
    }
    else
    {
        printf("No se pudo.");
    }
    printf("%d", resultado);*/
    return 0;
}
